table inet regionlock
delete table inet regionlock
table inet regionlock {
    set pop_fra {
        type ipv4_addr
        elements = { 1.2.3.4, 5.6.7.8 }
    }
    chain out {
        type filter hook output priority filter; policy accept;
        ip daddr @pop_fra meta l4proto udp drop
    }
}
